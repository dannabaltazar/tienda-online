# PostgreSQL configuration for local or Render deployment
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=tu_password
DB_NAME=tienda_online
PORT=3000
# Optional: use a full DATABASE_URL instead of individual values
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/tienda_online_db
